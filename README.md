# codehealth-demo

A deliberately imperfect JavaScript project used to test the CodeHealth
analysis pipeline end to end.

It contains a handful of intentional issues so the analysers have something
to find: an `eval` call, an unused variable, a deeply nested function and a
duplicated block.
